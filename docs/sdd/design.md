# 桃屁屁 2.0 Design

## 1. 视频素材映射

| 标准名 | 用户原视频 | 运行状态 | 播放策略 |
| --- | --- | --- | --- |
| `greeting` | `2457…打招呼.mp4` | 首次启动、合格悬停/点击 | 单次播放，结束停回待机，不循环 |
| `toilet` | `6419…飞奔…厕所.mp4` | 完成厕所提醒 | 单次播放，结束回当前主状态 |
| `focus` | `6475…坐在凳子上…电脑.mp4` | 专注 | 仅循环安静敲电脑片段，裁掉接缝尾帧 |
| `sleep` | `9784…睡觉.mp4` | 番茄休息、有效休息 | 循环稳定睡眠段，避开动作重置帧 |
| `pressure` | `9810…坐着…爆炸.mp4` | 压力预警、全屏爆炸 | 前段按压力定位；爆炸只播放爆开瞬间 |

原始 MP4 位于 `assets/video/source/`，透明 WebM 位于 `assets/video/generated/`；时间元数据在 `assets/video/manifest.json`。完整处理方法见 `docs/video-asset-workflow.md`。

## 2. 视频标准化

### 2.1 去背与编码

1. FFmpeg 抽帧并统一帧率和工作宽度。
2. `rembg` 的 `u2netp` 模型逐帧生成 Alpha，保留主体和必要道具。
3. 对每段素材计算非透明像素包围盒，按脚底/椅脚基线建立统一锚点。
4. 放入固定透明画布，角色可视高度统一，避免悬停切换时突然变小。
5. 编码为无音轨 VP9 `yuva420p` WebM，关闭 `auto-alt-ref` 以保留 Alpha。

运行时不通过 CSS 改变视频本体尺寸来模拟状态；角色膨胀只由压力动作或统一容器中的受控缩放完成。

### 2.2 裁头尾和爆炸

- 问候：去掉动作开始前和结束后相似的停顿帧，单次播放后回待机。
- 专注/睡觉：用相邻帧差寻找动作最接近的首尾点；循环结束点必须早于明显“回到开场姿势”的帧。
- 压力：前段用于压力预览；全屏爆炸从桃体破裂前数帧开始，到爆裂主体离开/收缩完成前结束。
- 最后一帧若只剩椅子，不进入正式文件。若椅子与爆裂瞬间重叠，则逐帧遮罩椅子区域后重新编码。

## 3. 角色状态机

视觉优先级：`exploding → deflated → reminder → break → pressure → focus → greeting → idle`。

- `idle`：透明待机图，静止；不使用持续 CSS 摇摆。
- `greeting`：应用启动自动播放一次；首次有效悬停可播放一次，随后进入冷却。播放完成自动恢复上一个主状态。
- `focus`：安静敲电脑。鼠标未悬停时无气泡、无额外动作。
- `pressure`：压力 55 以上进入，按压力值映射到变红/膨胀进度，不循环完整爆炸段。
- `break`：用户点击确认休息后播放睡觉视频。
- `reminder`：用短气泡提示；完成厕所提醒时播放厕所视频。
- `exploding`：独立全屏透明窗口仅播放爆开段和扣分提示。
- `deflated`：爆炸结束回小窗；健康行为推动恢复值分段增长。

状态切换使用短淡入淡出，但媒体容器尺寸和底部锚点保持不变。`prefers-reduced-motion` 下关闭自动悬停问候，只保留必要状态动作。

## 4. 桌宠窗口与交互

### 4.1 尺寸

- 默认角色约 170–190px；可调范围 140–320px。
- 调节只改变透明宠物窗口/媒体容器，不缩放右键菜单。
- 右键菜单放在独立固定尺寸浮层或独立窗口；空间不足时自动翻转到另一侧。

### 4.2 左键和悬停

- 当前有提醒：左键完成提醒并播放反馈。
- 番茄已结束：左键确认开始休息。
- 无待处理事项：左键只做短互动。
- 专注中悬停：气泡文案为“还剩 24:18，我陪你一起完成”。
- 非专注悬停：显示当前状态；满足冷却条件时问候视频单次播放。
- 气泡离开后隐藏，倒计时绝不放在宠物底部。

### 4.3 分级右键菜单

第一层保持 4–6 个清晰入口：

| 一级入口 | 二级内容 |
| --- | --- |
| 专注 | 开始/暂停/继续、25/45/60 分钟、自定义 |
| 健康打卡 | 喝水、起身、护眼、厕所 |
| 外观 | 只调宠物大小、恢复默认 |
| 桃桃小屋 | 打开统计页 |
| 更多 | 提醒设置、隐藏、退出 |

点击一级入口后才打开对应子面板；返回按钮回第一层。菜单字体和点击区固定，不跟随宠物缩小。

## 5. 倒计时与平台适配

- macOS：托盘/菜单栏标题显示 `24:18`；暂停时显示暂停标识，非专注时隐藏时间。系统菜单栏字体由系统控制，应用选择短、粗、易辨认的数字排版。
- Windows：任务栏进度反映完成比例；托盘悬停显示“桃屁屁 · 专注剩余 24:18”。如数字叠加图标可读性不足，以托盘提示为正式降级方案。
- 桌宠：专注时悬停气泡显示剩余时间。

## 6. 能量、压力与恢复

每天首次启动或跨日从 50 分开始，范围 0–100。专注时长只进入统计，不直接奖励分数。

| 行为 | 分数 | 压力变化 |
| --- | ---: | ---: |
| 喝水 / 护眼 / 厕所 | +5 | -12 |
| 起身活动 | +8 | -20 |
| 完成有效番茄休息 | +8 | -25 |
| 键鼠空闲满 3 分钟 | +3，每日最多 5 次 | -15 |
| 忽略提醒 | -3 | +10 |
| 当日第 1 / 2 / 3 次爆炸 | -15 / -25 / -40 | 清空压力并进入瘪气 |

瘪气恢复值为 0–100。完成健康行为后累加，达到 33/66/100 时切换恢复阶段；恢复至正常后仍受健康分与压力规则约束。

## 7. 桃桃小屋视觉与排版

### 7.1 色彩

- 主桃色：提高明度和饱和度，用于角色、数字和交互重点。
- 珊瑚橙：用于能量弧、趋势线、激活状态。
- 叶绿：用于健康完成、能量增长和叶片。
- 奶油白：用于文字承载层和模态内容。
- 房间背景降低约一个对比层级，避免与角色争抢注意力。

所有文本与承载面至少满足可读对比度；不在复杂生成图上直接放小号正文。

### 7.2 字号层级与安全区

- 能量数字：64–88px；页面主标题：30–36px；区块标题：22–26px。
- 关键指标：32–44px；正文：17–20px；辅助说明不低于 14px。
- 生成图仅作为背景或独立图像元素；文字放在可控的布局容器中。
- 任何文字距离资产边缘至少 20px，窗口缩小时允许换行或隐藏次要说明，不允许溢出。

### 7.3 页面结构

1. 左上品牌；右上日期和低频入口。
2. 中上能量弧与大号分数。
3. 其下为今日专注、休息、活跃三项摘要。
4. 中下为 7 天成长路线，数据点使用桃子节点。
5. 底部为健康行为快捷入口。
6. 右侧角色只在首次进入或指针悬停时播放一次动作；其余时间保持安静。
7. “今日的话”只展示一个标题/短摘要入口，点击弹出固定尺寸、可滚动的内容层；文案长度不再影响主布局。

## 8. 图像资产与生成提示词

参考图：`/Users/yangzhou/.codex/generated_images/01a01d5f-e8c8-7dd0-ba6a-e23840644704/exec-fa04e291-294c-46bf-bc47-a82186114145.png`

统一提示词前缀：

> 严格参考所附桃桃小屋设计，单个独立 UI 道具，温暖粉橙与草地绿配色，精致 3D 漫射哑光材质，轻质感，高级、简约、低 AI 感，固定正面略俯视镜头，柔和自然光，造型圆润但不过度玩具化，不含文字、数字、边框、额外物体；除房间背景外输出透明背景 PNG，主体完整且四周留安全边距。

| 路径 | 追加提示词 / 用途 |
| --- | --- |
| `assets/dashboard/room-background.png` | 空的温暖工作室，左侧书和绿植、右侧窗户，中间留出大面积干净排版区；不含角色和 UI |
| `assets/dashboard/energy-arc.png` | 珊瑚桃色与叶绿组成的半圆能量弧，顶部一片叶子，中间完全留空 |
| `assets/dashboard/water.png` | 半透明蓝色水瓶，白色水滴标记 |
| `assets/dashboard/stand.png` | 小型木质圆台与嫩绿芽，表达起身活动 |
| `assets/dashboard/eye-mask.png` | 叶绿色柔软护眼/睡眠眼罩 |
| `assets/dashboard/toilet.png` | 奶油白迷你马桶，桃粉和叶绿点缀 |
| `assets/dashboard/calendar.png` | 奶油白日历，珊瑚色装订和低细节绿色勾选，不出现可识别文字 |
| `assets/dashboard/timer.png` | 粉桃色圆角番茄钟外壳，深色空白屏幕和叶绿空白按钮，数字由程序叠加 |
| `assets/dashboard/story-note.png` | 奶油白悬挂便签板，桃色夹子，纸面完全留白，用作今日的话入口 |
| `assets/dashboard/motivation-note.png` | 小型奶油白鼓励便签，桃色图钉，纸面留白 |
| `assets/dashboard/milestone.png` | 小桃子形路线节点，顶部嫩叶，中间留出数字区域 |

桌宠角色静态图仍遵循 `docs/assets/image-prompts.md`，必须参考 `桃屁屁_绿色方案3_草地绿.png` 的身材和腿长，不用 HTML/CSS/SVG 造角色。

## 9. 数据与降级

- SQLite 继续保存设置、事件和 7 天统计；新增/持久化宠物大小、恢复值与自定义专注时长。
- 视频加载失败时回退到对应透明 PNG，并记录素材失败事件。
- Alpha WebM 不兼容时回退 PNG，不显示白/灰色矩形视频背景。
- 全屏爆炸窗口无论正常结束或异常关闭，都必须恢复小窗可交互状态。

## 10. 验收测试矩阵

- 单元：50 分跨日重置、所有加扣分、瘪气恢复、状态优先级、尺寸上下限、菜单导航。
- 素材：5 个 WebM 存在、VP9、Alpha、无音轨、时间段合法、统一包围盒。
- 视觉：首尾接缝、悬停尺寸一致、椅子尾帧、文本溢出、最小窗口布局。
- 集成：专注→完成→点击休息→睡觉；压力→爆炸→瘪气→恢复。
- macOS：透明置顶、多桌面、菜单栏倒计时、缩放、全屏爆炸。
- Windows：透明窗口、任务栏进度、托盘倒计时、安装/卸载。
