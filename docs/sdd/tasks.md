# 桃屁屁 2.0 Tasks / 验收清单

> 标记规则：`[x]` 已落地到工作区；`[ ]` 尚未完成或仍需实机验证。功能完成前不因“已有代码”提前勾选体验验收。

## A. 视频素材流水线

- [x] 复制 7 个原始 MP4 到 `assets/video/source/` 并使用稳定英文名。
- [x] 建立 `rembg u2netp` 逐帧去背和 VP9 Alpha WebM 编码脚本。
- [x] 输出 12 个透明 WebM 到 `assets/video/generated/`（含待机、护眼、活动和爆炸独立动效）。
- [x] 建立视频清单和 VP9/Alpha/无音轨校验脚本。
- [x] 为每段视频写入最终裁头、裁尾和循环时间，经逐帧检查确认。
- [x] 统一 8 段视频透明画布、主体高度、脚底/椅脚锚点，消除悬停缩小。
- [x] 删除专注循环的割裂尾帧；专注只循环安静敲电脑段。
- [x] 将爆炸裁为仅包含爆开瞬间的短片；最终帧不得保留椅子。
- [x] 爆裂瞬间使用颜色/轮廓遮罩清除椅子，尾帧不再显示空椅子。
- [x] 扩展素材校验：画布、帧率、时间线、脚底安全区、Alpha 浅边和休息动作映射。
- [x] 深色/棋盘背景 contact sheet 逐时间线检查脚部、道具和透明边缘。

## B. 健康核心逻辑

- [x] 每日初始健康分改为 0；连续工作不加分，总分无固定上限。
- [x] 增加按行为差异加分、每日单项计分上限、忽略提醒和分级爆炸扣分。
- [x] 增加瘪气恢复值和分段恢复逻辑。
- [x] 增加桌宠视觉状态计算器与优先级测试。
- [x] 将默认宠物尺寸改为 140px、最小 120px，并移除尺寸选择入口。
- [x] 内部保留一次性回滚能力用于数据兼容，但按最新交互要求移除桌宠气泡和右键菜单中的撤销入口。
- [ ] 验证跨午夜运行时能量从新一天的 0 分开始，且旧日统计正确封账。
- [ ] 验证当日第 1/2/3 次爆炸分别扣 15/25/40 分。

## C. 桌宠播放与交互

- [x] 默认桌宠缩小，移除底部常驻倒计时与持续摇摆。
- [x] 接入挥手、专注、睡觉、厕所和压力/爆炸视频播放器，失败回退 PNG。
- [x] 专注时悬停显示剩余时间气泡。
- [x] 取消 UI/右键里的尺寸选择；已保存的历史尺寸仍兼容读取。
- [x] 问候仅在用户明确点击“打招呼”时完整播放 9.92 秒，不再启动/悬停自动抢注意力。
- [x] 角色视频统一为同尺寸透明画布，状态切换不再改变窗口或媒体槽尺寸。
- [x] 将右键菜单改为“一级分类→对应子菜单”，不再一次显示所有选项。
- [x] 菜单使用系统固定可读尺寸；调节宠物大小时菜单大小不变。
- [x] 专注开始/取消先完整播放变身视频（保留到 9.82s 的旋风尾部、1.55 倍速、尾部淡出）；专注态点击只鼓励并保持敲电脑状态。
- [x] 水提醒超过 15 分钟时停在干裂姿势；确认喝水后才播放喝水恢复段。
- [x] 每个番茄结束的当次休息都轮播活动、喝水、厕所、护眼；打卡项立即移除，未打卡项在本次继续播放。
- [x] 增加独立的气泡空间；气泡只显示一句短文案、3.2 秒自动隐藏，无按钮，不覆盖或裁切宠物脚部和椅脚。
- [x] 新增低频待机 WebM：眨眼、三指小手和短腿只做微小动作，主体尺寸与脚底基线固定。
- [x] 新增护眼超时四级动作：眼睛逐步变红、干裂，最后停在警示帧。
- [x] 全部旧视频统一提亮增色；打招呼素材清除脚内白影与低 Alpha 毛边；专注椅脚增加 18px 安全区。
- [ ] 菜单在屏幕边缘自动翻转，键盘可操作，Esc 返回或关闭。
- [ ] 番茄结束后点击桃屁屁确认休息，并给出明确单次反馈。

## D. 平台计时与爆炸

- [x] macOS 菜单栏在专注中显示倒计时。
- [x] Windows 任务栏显示专注完成进度。
- [x] 建立独立全屏透明爆炸窗口。
- [ ] macOS 菜单栏验证暂停、继续、结束和数字可读性。
- [ ] Windows 托盘提示显示完整剩余时间；任务栏进度在暂停/结束时正确恢复。
- [ ] 爆炸窗口只播放爆开短片，无椅子尾帧，完成后回到瘪气小窗。
- [ ] 多显示器下在宠物所在屏幕爆炸，而不是固定主屏幕。
- [ ] 爆炸窗口异常关闭后仍恢复小窗和鼠标交互。

## E. 桃桃小屋统计页

- [x] 使用独立 ImageGen 资产搭建房间、能量弧、健康道具、便签、计时器和桃子节点。
- [x] 建立粉桃、珊瑚橙、叶绿、奶油白视觉基调。
- [x] 提高角色和关键桃色的明度/饱和度，降低背景对比，确保主体突出。
- [x] 按设计稿重排能量、三项摘要、7 天成长路线、快捷行为和角色区域。
- [x] 建立统一字体层级：能量数字、标题、指标、正文、辅助文字不得同级过小。
- [x] 修复所有文字超出生成图片/卡片边界的问题。
- [x] “今日的话”改为可点击入口，弹出固定尺寸内容层。
- [x] 统计页角色只在初次进入或悬停时单次播放，不持续循环。
- [x] 默认窗口和高 DPI 下完成实机截图对比；保留 `960 × 650` 最小尺寸保护。
- [x] 完成 Product Design 视觉 QA，记录 P0/P1/P2 并达到 `design-qa.md: passed`。
- [x] 统计页改为从 0 分的累计能量；趋势纵轴随数据增长，底部图标放大、文字限制在素材安全区内。
- [x] 将统计页重构为统一响应式网格，修复能量数字/标题相压、折线不随宽度变化、宽屏和最小窗口底部标签越界。
- [x] 在 `960 × 650`、`1050 × 760`、`1400 × 800` 三档 macOS 实机窗口完成截图对比。
- [x] 修正“今日的话”和 7 天成长路线标题的居中约束，在 `960 × 650` 与 `1050 × 760` 再次实机截图确认无重叠。
- [x] 新增当前自然月健康日历，按周一开始补齐 35/42 格，并为无记录日期补零。
- [x] 成长卡支持“7 天 / 本月”原位切换；点击日期可查看当日四项行为、专注、活跃和能量。
- [x] 删除统计页“每日一句”、番茄计时器和独立“休息一下”入口，底部行为坞固定四等分。
- [x] 将“起身”统一改为“活动一下”，使用 ImageGen 生成的透明磨砂拉伸人物资产。
- [x] 在 `960 × 650` 与 `1050 × 760` 实机验证月历七列、卡片、文字和图片无重叠或越界。

## F. 文档与新增视频复用

- [x] 更新 Proposal/PRD、Design 与任务清单。
- [x] 新增 `docs/video-asset-workflow.md`，记录抽帧、去背、尺寸归一、裁尾、编码与校验。
- [x] 记录 `transform` 的完整旋风段与 1.25 倍速，以及 `dry → hydrating` 的“先干裂、确认后恢复”分段播放规则。
- [x] 记录统计页图像资产路径和可复用生成提示词。
- [ ] 用下一段用户视频完整演练一次新增素材流程，验证文档无缺步。

## G. 发布前回归

- [x] `npm run assets:check`
- [x] `npm run videos:check`
- [x] `npm run typecheck`
- [x] `npm test`
- [x] `npm run build`
- [ ] macOS 透明、置顶、拖动、左键、悬停、分级右键、缩放、菜单栏、爆炸全流程。
- [ ] Windows 10/11 透明、任务栏、托盘、安装、启动、卸载全流程。
